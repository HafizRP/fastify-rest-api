{
  "name": "products-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "dotenv -v NODE_ENV=production -- node dist/server.js",
    "migrate:prod": "dotnenv -e .env -- npx prisma migrate dev",
    "migrate:dev": "dotenv -e .env.local -- npx prisma migrate dev",
    "dev": "dotenv -v NODE_ENV=development -- tsnd --respawn --transpile-only --exit-child src/server.ts",
    "pretest": "dotenv -e .env.test -- npx prisma migrate dev",
    "test": "dotenv -e env.test -v NODE_ENV=test -- jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/jwt": "^7.2.0",
    "@fastify/swagger": "^8.8.0",
    "@fastify/swagger-ui": "^1.9.3",
    "@prisma/client": "^5.0.0",
    "@types/jest": "^29.5.3",
    "fastify": "^4.20.0",
    "fastify-jwt": "^4.2.0",
    "fastify-plugin": "^4.5.1",
    "fastify-zod": "^1.3.2",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "zod": "^3.21.4",
    "zod-to-json-schema": "^3.21.4"
  },
  "devDependencies": {
    "@types/fastify-jwt": "^0.8.1",
    "@types/node": "^20.4.5",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.2.1",
    "typescript": "^5.1.6"
  }
}
